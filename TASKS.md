# TASKS — งานที่ต้องทำ / ต้องเตือน

_จารดูแลไฟล์นี้ · เช็กตอนเริ่มทุก session · ผู้ใช้บอก "เตือนเรื่อง X วันที่ Y" = จดที่นี่_
_อัปเดตล่าสุด: 2026-09-08_

รูปแบบ: `- [ ] (กำหนด: YYYY-MM-DD | ไม่มี) <งาน> — <โน้ต>` · เสร็จแล้วเปลี่ยนเป็น `- [x]` แล้วลบออกตอนรีวิว

---

## งานโปรเจค

### The Bright
- [ ] (กำหนด: ไม่มี) ตัดสินใจเรื่อง **RLS** — 6 ตาราง (`crew`, `teaching_logs`, `rooms`, `schedules`, `schedule_notifications`, `school_settings`) เปิด RLS ไม่ครบ = anon key อ่าน/แก้ได้ตรงๆ ต้องออกแบบ policy ก่อนเปิด ไม่งั้นแอปพัง · ผู้ใช้บอก "รอก่อน" 2026-09-05
- [ ] (กำหนด: ไม่มี) เทสต์บนเว็บจริงหลัง deploy: ครูคีน ส.ค. ต้องขึ้น **18 คาบ/ชม.** + ยอดรวมค่าสอน ส.ค. = **196,920** (หลังแก้ 1000-row pagination `2ba9dfe`)
- [ ] (กำหนด: ไม่มี) เทสต์บน **มือถือจริง**: ลากการ์ดตาราง (กดค้าง 250ms), double-tap แก้, auto-scroll, ถาดพักไม่ทับปุ่มแชท — deploy แล้วยังไม่ยืนยัน

### Prism Lab
- [ ] (กำหนด: ไม่มี) **D2** — เคาะกรอบราคา setup fee + ค่ารายเดือน (research แนะนำรายเดือน ~฿1,000–1,800)
- [ ] (กำหนด: ไม่มี) **D3** — เกณฑ์เลือกกวดวิชา 2–3 เจ้าแรกไป validate
- [ ] (กำหนด: ไม่มี) ลงมือ Tier 1 config refactor บน `prism-lab/app/` (`prism-agent`) → ปลดล็อก demo "Apex Learning"

---

## งานส่วนตัว
- (ยังไม่มี — ผู้ใช้บอกมาได้เลย)

---

## รอคนอื่น
- (ยังไม่มี)

---

## ไอเดีย / ค้างไว้
- แชทบอทช่วยเหลือใน **BTK** — คุยแนวทางไว้แล้ว (Supabase Edge Function + widget ในไฟล์เดียว, ไม่มี login ต้อง rate-limit) ยังไม่เริ่มทำ
- สรุปงานตอนเช้าแบบ cloud (ตั้ง scheduled agent ส่งเข้ามือถือ) — เสนอไว้ ผู้ใช้ยังไม่เอา
